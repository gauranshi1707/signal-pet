<Window x:Class="SignalPet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Signal Pet — Detection proof of concept"
        Width="560"
        Height="260"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock FontSize="20" FontWeight="SemiBold" Text="Signal Pet notification detector" />
    <TextBlock Grid.Row="1" Margin="0,18,0,0" TextWrapping="Wrap" Text="Preparing notification access…" x:Name="StatusText" />
  </Grid>
</Window>
